# 项目维护台账

最后更新：2026-05-02
维护人：Codex

## 2026-05-02 Debian / Docker 适配记录

### 已完成
1. 新建 `debian-docker` 分支，用于隔离 Debian 容器化改造
2. 完成首轮可行性评估，确认核心运行层可迁移到 Debian
3. 新增 `Dockerfile`
4. 新增 `docker-compose.yml`
5. 新增 `docker/entrypoint.sh`
6. 新增 `.dockerignore`
7. 为 `package.json` 补充基础启动脚本
8. 在 `README.md` 补充 Debian / Docker 启动与部署说明

### 进行中
1. 验证 Docker 构建与容器启动是否能在当前环境直接通过
2. 观察 Debian 第一版是否还需要补充额外兼容项

### 待完成
1. 如有需要，补 Linux 版初始化脚本，替代 `init.bat`
2. 视运行结果决定是否补充 `.gitignore`
3. 视运行结果决定是否修复 `main.js` 中与 Docker 无关的历史接口问题

---

## 2026-05-02 文档拆分记录

### 已完成
1. 将根目录大体量 `README.md` 拆分为 `docs/` 分类文档
2. 新建根目录精简导航版 `README.md`
3. 新建 `docs/README.md` 文档目录
4. 按主题拆分出 6 份文档：
   `01-overview-and-init.md`
   `02-startup-and-deploy.md`
   `03-config-and-auth.md`
   `04-features-and-logic.md`
   `05-web-console-api.md`
   `06-data-troubleshooting-and-maintenance.md`

### 进行中
1. 观察拆分后的文档结构是否还需要再细分
2. 后续新增文档统一收口到 `docs/`

### 待完成
1. 如后续继续扩展 Docker / Debian 内容，可单独拆成独立文档
2. 如 Web 操作手册继续膨胀，可再从 `05-web-console-api.md` 中拆出 `web-manual.md`

---

## 1. 本轮工作范围

本轮主要处理项目文档整理，不涉及业务代码逻辑调整。

目标：
1. 重新整理项目说明文档，形成统一维护入口
2. 把配置、启动、功能逻辑、Web 操作说明、排错建议补齐
3. 建立一份可持续追加的维护台账，记录已完成、进行中、待完成事项

---

## 2. 已完成

### 2.1 新建主文档 `README.md`

已在仓库根目录新增主文档：
- [README.md](/c:/Users/Lucas/aiman/README.md)

本次已补齐的内容包括：
1. 项目定位与整体架构
2. 目录结构与关键文件职责
3. 环境要求与依赖说明
4. 初始化流程与初始化脚本作用
5. 启动方式、启动参数、环境变量
6. 配置来源、优先级与推荐修改方式
7. 账号规则、`110001` 前缀、AAA / GS 状态说明
8. 核心功能说明
9. 世界喊话与世界组队的实现逻辑
10. Web 后台操作手册
11. 控制台 / REPL 常用命令
12. HTTP API、数据文件、日志与排错建议
13. 已知问题、维护建议与推荐维护流程

### 2.2 收口旧文档入口

已将旧文档改为入口提示，避免继续维护两份内容：
- [readme.txt](/c:/Users/Lucas/aiman/readme.txt)

当前策略：
1. `README.md` 作为唯一主文档
2. `readme.txt` 仅保留跳转说明
3. 后续新增说明优先写入 `README.md`

### 2.3 已沉淀的关键维护结论

这轮文档整理过程中，已经确认并写入主文档的关键事实包括：
1. 运行时实际配置优先读取 `config/config.json`，不是单纯看 `cfg.js`
2. Web 服务端口优先受 `HTTP_PORT` 或运行时配置影响
3. 世界喊话组队不是“命中关键词就一定进队”，中间还有条件判断与主动申请流程
4. AAA 与 GS 的连接状态不能简单等价理解为“在线 / 离线”
5. 页面统计与实际连接状态之间存在历史实现上的误差风险
6. 项目存在编码历史包袱，部分终端或编辑器下会出现中文乱码

---

## 3. 进行中

### 3.1 文档体系进入单点维护

当前已经完成主文档收口，但这件事本身会持续进行：
1. 后续代码逻辑如果有调整，需要同步更新 `README.md`
2. 后续新增接口、页面按钮、配置项，也需要持续补到 `README.md`
3. `WORKLOG.md` 需要作为每轮维护的追加记录，不要中断

### 3.2 现有代码状态仍在观察

从当前工作区状态看，以下文件已有修改，但不属于本轮文档任务直接变更：
1. `Instruction.js`
2. `cfg.js`
3. `config/config.json`
4. `data/list.json`
5. `main.js`
6. `me.js`
7. `version.js`

说明：
1. 这些改动可能是你当前调试中的工作，也可能是历史未提交改动
2. 本轮没有回退或覆盖这些文件
3. 如果后面要做功能修复，建议先基于这些现状继续分析，不要直接按旧版本文档理解

---

## 4. 待完成

### 4.1 建议后续补充的文档项

如果你希望文档继续升级，后面可以补这些内容：
1. Web 页面截图版操作手册
2. 常用调试场景示例
3. 世界喊话 / 组队 / 自动任务的时序图
4. 常见配置模板
5. 多实例启动示例
6. 常见日志关键字对照表

### 4.2 建议后续跟进的代码问题

以下是本轮整理时发现、但未在本轮处理的事项：
1. `main.js` 中存在一个疑似历史笔误：`/api/loginListClient` 处疑似使用了 `res.join(...)`
2. 代码与页面存在一定乱码历史问题，后续可能需要统一编码
3. 运行时配置与静态配置并存，后续可考虑继续收口，减少维护歧义

### 4.3 建议建立的维护流程

后续每次改动，建议最少补三项记录：
1. 改了什么
2. 为什么改
3. 影响了哪些文件 / 配置 / 页面行为

---

## 5. 本轮实际变更清单

本轮由我新增或修改的文档文件：
1. [README.md](/c:/Users/Lucas/aiman/README.md)
2. [readme.txt](/c:/Users/Lucas/aiman/readme.txt)
3. [WORKLOG.md](/c:/Users/Lucas/aiman/WORKLOG.md)

本轮未处理：
1. 业务代码修复
2. 接口联调
3. 页面逻辑改造
4. 配置热更新机制

---

## 6. 下次续写建议

下次继续维护时，建议直接在本文件顶部新增一段新记录，格式可参考：

```md
## 2026-05-02 第二轮记录

### 已完成
1. ...

### 进行中
1. ...

### 待完成
1. ...
```
